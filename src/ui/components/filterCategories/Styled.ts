import Multiselect from 'multiselect-react-dropdown';
import styled from 'styled-components';

export const FilterMultiCategories = styled(Multiselect)`
  background-color: #ffffff;
  min-width: 150px;
  font-size: 18px;
  width: 100%;
  height: auto;
  margin-top: 20px;
  &:hover {
    cursor: pointer;
  }
`;
